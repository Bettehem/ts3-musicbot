image: "gradle:jdk8"

before_script:
  - apt-get install zip
  - curl -s https://get.sdkman.io | bash
  - source "$HOME/.sdkman/bin/sdkman-init.sh"
  - sdk install kotlin

build:
  script:
    - gradle build